/**
 * A montagem do cardápio vive em @vendas-bot/shared para que API e frontend
 * usem exatamente a mesma implementação. Reexportado aqui por conveniência
 * dos módulos da API.
 */
export { getPublicMenu, parseLocation } from '@vendas-bot/shared'
